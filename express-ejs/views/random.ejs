<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>random passing data ke template</title>
</head>
<body>
    <h1>Random Number : <%= num %></h1>
    <%  if (num % 2 === 0) {  %>
        <h3>Angka Genap</h3>
    <%  } else {  %>
        <h3>Angka Ganjil</h3>       
    <% } %> 

    <!-- ternary if-->
        <h3> That number is : <%= num%2 === 0 ? 'even' : 'odd'  %></h3>
    
</body>
</html>

<!-- if (num % 2 === 0) {
    huruf Genap
} else {
    huruf Ganjil
} -->